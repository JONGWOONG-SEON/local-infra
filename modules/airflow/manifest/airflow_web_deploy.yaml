apiVersion: apps/v1
kind: Deployment
metadata:
  name: "airflow-web"
  namespace: "default"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "airflow-web"
  template:
    metadata:
      labels:
        app: "airflow-web"
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: "airflow"
                    operator: In
                    values:
                      - "true"
      containers:
        - name: "airflow-web"
          image: "localhost:5000/custom-airflow:0.1"
          imagePullPolicy: IfNotPresent
          command: ["/bin/bash", "-c"]
          args:
              - "rm -f $AIRFLOW_HOME/airflow-webserver.pid && airflow db upgrade && exec airflow webserver"
          envFrom:
            - configMapRef:
                name: "airflow-env"
          volumeMounts:
            - name: "dags-main"
              mountPath: "/opt/airflow/dags"
      volumes:
        - name: "dags-main"
          persistentVolumeClaim:
            claimName: "airflow-pvc"
