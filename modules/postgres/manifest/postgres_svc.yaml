apiVersion: v1
kind: Service
metadata:
  name: "postgres-svc"
  namespace: "default"
spec:
  type: NodePort
  selector:
    app: "postgres"
  ports:
    - name: postgres-svc
      protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30432